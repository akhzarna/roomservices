# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Room Services' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Room Services
   pod 'IQKeyboardManagerSwift'
   pod 'SDWebImage', '~> 5.0'
   pod 'Alamofire', '~> 4.5'
   pod "KRProgressHUD"
   pod 'OAuthSwift', '~> 2.0.0'
   pod 'moa', '~> 11.0'
   pod 'Auk', '~> 10.0'
   pod 'FMDB'

  target 'Room ServicesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Room ServicesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
